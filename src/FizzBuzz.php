<?php

namespace App;

class FizzBuzz
{
    /**
     * Process the numbers from 1 to 100
     *   - Create an array containing indexes for each number
     *   - For multiples of three, set the value of that index to "Fizz"
     *   - For multiples of five, set the value of that index to "Buzz"
     *   - For multiples of both three and five, set the value of that index to "FizzBuzz"
     *   - Otherwise, set the value of the index to the number
     *
     * @var string $string
     * @return array
     */
    public function run(): array
    {
        // TODO: Implement
        return [];
    }
}
